# Copy to backend/.env and fill in values. Do not commit backend/.env.

# Required for classification (Jina Omni) and field extraction (Jina VLM).
JINA_API_KEY=

# Optional OCR harness. Must be https. Example path: https://your-host/api/parse
JINA_OCR_URL=
