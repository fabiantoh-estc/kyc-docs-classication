# Copy this file to backend/.env (never commit backend/.env).
# Get a Jina API key at https://jina.ai
JINA_API_KEY=

# Optional. HTTPS URL of an OCR parse endpoint that accepts multipart file uploads
# (POST .../api/parse with form field "file"). Leave empty to use Jina VLM extraction only.
JINA_OCR_URL=
